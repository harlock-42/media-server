version: '3'
services:
  nestjs-app:
    container_name: nestjs-app
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    volumes:
      - ./videos:/app/videos
    environment:
      - NODE_ENV=production
